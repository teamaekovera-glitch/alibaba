packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks dependency build scripts by default; allowlist what needs
# to run: Prisma engine downloader, esbuild/unrs-resolver binaries.
onlyBuiltDependencies:
  - "@prisma/client"
  - "@prisma/engines"
  - "esbuild"
  - "prisma"
  - "unrs-resolver"
