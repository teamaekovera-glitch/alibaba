# obvious.md — teamaekovera-glitch/alibaba

## Status

This repository is currently **empty**. The only tracked file is `README.md`, which contains only the repo title.

## What is here

- No dependency manifests (package.json, go.mod, Cargo.toml, pyproject.toml, Gemfile, etc.)
- No Dockerfiles, Compose files, or Makefiles
- No source code outside README.md

## Local development

Not applicable — there is no application to run yet. No commands, ports, or stack claims are recorded because none exist.

## Next steps

The next onboarding/setup run should fill in this file with the actual stack, commands, codebase map, and local verification steps once code lands. `codebase-map.md` and `skills/local-dev/SKILL.md` were deliberately omitted on this run (nothing to map, no local dev to record).
