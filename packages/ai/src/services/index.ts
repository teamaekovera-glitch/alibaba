/**
 * AI services (spec: AI services table) — deterministic value pipelines over
 * the adapter kit. Each service follows the spec-extraction pattern: values
 * come from code (so builds, tests, and demos run keyless and reproducible),
 * the reasoning adapter supplies provenance and narrative, and deploy-time
 * credentials deepen the pipeline behind unchanged shapes.
 */
export * from "./rfq-draft";
export * from "./compliance";
export * from "./translate";
